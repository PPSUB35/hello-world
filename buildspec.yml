version: 0.2

phases:
  install:
    commands:
      - echo "Hello, and, Goodbye! (phase=install)"
  pre_build:
    commands:
      - echo "Hello, and, Goodbye! (phase=pre_build)"
  build:
    commands:
      - echo "Hello, and, Goodbye! (phase=build)"
      - ls -al
  post_build:
    commands:
      - echo "Hello, and, Goodbye! (phase=post_build)"
